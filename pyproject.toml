[build-system]
requires = ["uv_build>=0.12.0,<0.13"]
build-backend = "uv_build"

[project]
name = "worklore"
version = "0.1.0"
description = "Small, opinionated engineering workflows for coding agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
worklore = "worklore.cli:main"

[tool.uv.build-backend]
module-root = ""
